language: node_js
node_js:
  - "node"
dist: trusty
addons:
  chrome: stable
before_install:
  - google-chrome-stable --headless --no-sandbox --disbale-gpu --remote-debugging-port=9222 http://localhost &
install:
  - npm install
script: 
  - npm test