import { PixiGraphicsWrapperDirective } from './pixi-graphicswrapper.directive';

describe('PixiGraphicswrapperDirective', () => {
    it('should create an instance', () => {
        const directive = new PixiGraphicsWrapperDirective();
        expect(directive).toBeTruthy();
    });
});
